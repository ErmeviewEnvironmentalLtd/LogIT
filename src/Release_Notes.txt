 - Updated to SHIP v0.2.5.
 - [BUGFIX/FEATURE] Added better support for Tuflow event files (tef). Previously
   only DEFINE EVENT was recognised as a usable command. Now supports the use
   of IF, ELSE IF, ELSE, and DEFINE in event logic.
